# ASTRA
ASTRA — an astronaut-styled ROS 2 robot that navigates a simulated spacecraft and grabs/fixes parts on natural-language command, built with Nav2, MoveIt-style arm control, and an LLM command interface.
